# SESSION RESTORE - King Wallet Accounting

## آخر جلسة مطلوبة

الجلسة التي طلب المستخدم الرجوع لها كانت تقريبًا من 11 ساعة، وهي:

`C:\Users\Zanaty\.codex\sessions\2026\04\22\rollout-2026-04-22T06-07-21-019db35f-02dd-7c21-ab80-bae1e3755096.jsonl`

بدأت تقريبًا:

`2026-04-22 06:07`

وآخر نشاط معروف فيها كان تقريبًا:

`2026-04-22 07:24`

## آخر نقطة وصلنا لها

تمت إضافة زر مباشر باسم **اعتماد** داخل تفاصيل العميل بجانب أي سطر عملية آجلة `pending`.

الملف الأساسي:

`lib/screens/customer_details_panel.dart`

النقاط المهمة:

- الزر يظهر فقط للعمليات الآجلة `pending`.
- عند الضغط عليه يستخدم نفس مسار الاعتماد الموجود:
  `onLineAction(line, _LineAction.confirmPending)`
- المسار يستدعي:
  `AppDb.instance.confirmPending(line.txnId!)`
- لم يتم تغيير منطق المحاسبة.
- لم يتم تغيير الأرصدة.
- لم يتم تغيير Ledger أو settlement أو claims.
- تم الاعتماد على الدالة الأصلية `confirmPending` لأنها تحمي من اعتماد العملية مرتين.

## نتيجة الاختبارات في آخر جلسة

تم تشغيل:

- `flutter test test/screen_heavy_smoke_test.dart`
- `flutter test test/screen_flows_test.dart`
- `flutter test`

والنتيجة في آخر جلسة:

`All tests passed`

## حالة المشروع التي يجب تذكرها

هذا مشروع Flutter محاسبي Production خاص بالمحافظ الإلكترونية.

الأولوية دائمًا:

- سلامة الأرصدة.
- عدم كسر منطق المحاسبة.
- عدم تعديل `engine` أو schema إلا لضرورة واضحة.
- أقل تعديل ممكن.
- قراءة الملفات المرتبطة بالمهمة فقط.

## عند فتح جلسة جديدة

ابدأ بقراءة هذا الملف أولًا، ثم اقرأ:

- `CODEX_PROJECT_MASTER.md`
- `CHAT_HANDOFF_LATEST.md`
- `AI_PROJECT_CONTEXT.md`

ثم افحص:

`git status --short`

مهم:

يوجد في المشروع تغييرات كثيرة من جلسات سابقة. لا ترجع أي تغيير ولا تمسحه إلا إذا طلب المستخدم ذلك صراحة.

## طريقة الرد للمستخدم

الرد يكون بالعربية البسيطة.

قبل أي تنفيذ:

1. قل ماذا فهمت.
2. قل ما الملفات التي ستقرأها.
3. قل ما الملفات التي قد تعدلها.
4. قل الخطر الأساسي.
5. نفذ أقل تعديل آمن فقط.

بعد التنفيذ:

1. قل ماذا تم.
2. قل الملفات التي تغيرت.
3. قل كيف يختبر يدويًا.
4. اقترح خطوة تالية صغيرة واحدة.
5. اقترح commit message.

## ملاحظة مهمة عن ظهور الشات في الواجهة

هذا الملف يحفظ سياق العمل داخل المشروع، لكنه لا يضمن أن واجهة Codex ستعرض نفس المحادثة القديمة في الشريط الجانبي.

إذا اختفت المحادثة من الواجهة، يمكن استعادة السياق بقراءة هذا الملف والجلسة المحفوظة أعلاه.
